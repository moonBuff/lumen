# Agent Harness v1 Overview

Agent Harness v1 coordinates a model client, workspace context, tool registry, memory, and task state into one repeatable local coding loop.

The task state records run identity, attempts, tool steps, stop reason, checkpoint id, and final answer so benchmark and recovery behavior can be audited after a run.
